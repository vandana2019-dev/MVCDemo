@model  IEnumerable<MVC02.Models.Employee>
@using MVC02.Models
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div style="font-family:Arial">
    <h2>Employee List</h2>
    <hr />
    <ul>
        @foreach (Employee employee in @Model)
        {
            <li>
                @Html.ActionLink(employee.Name, "Details", new { id = employee.EmployeeId })
            </li>
        }
    </ul>

</div>

